# Galerie réentraînement — dépendances
# Validé sous Python 3.11+ (juillet 2026). Tout tourne sur CPU.
#
#   pip install -r requirements.txt
#   (avec uv : uv pip install -r requirements.txt)

scikit-learn>=1.4
pandas>=2.0
numpy>=1.26
matplotlib>=3.8
seaborn>=0.13
joblib>=1.3
jupyter>=1.0
